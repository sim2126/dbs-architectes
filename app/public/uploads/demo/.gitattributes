# Demo fixtures — byte-exact, no end-of-line translation.
#
# These are generated by prisma/seed-ai-files.ts and the seeded attachment
# rows record each file's exact byte length. More importantly the PDFs are
# uncompressed, so git classifies them as text: CRLF translation would shift
# every offset in the xref table and corrupt them on checkout.
* -text
